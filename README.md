# sameer-codes-ai.github.io

This site is built with Vite and React and is configured for GitHub Pages deployment.

## Deploying

- Push changes to the `main` branch.
- GitHub Actions will build the app and publish the contents of `dist/` to GitHub Pages.
- For local preview, run `npm install` and `npm run dev`.# sameer-codes-ai.github.io
